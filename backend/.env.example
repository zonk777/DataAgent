# 应用配置
APP_NAME=DataAgent
ENVIRONMENT=development
SECRET_KEY=replace-with-a-long-random-string
DATABASE_PATH=storage/data_agent.db
FRONTEND_ORIGIN=http://localhost:5173

# 大模型配置（留空时自动使用本地演示分析器）
# 支持 OpenAI 兼容的 Chat Completions 接口。
LLM_API_KEY=sk-46cf80e3c5124a12a3523d11dfde2851
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-flash

# Embedding 配置（首版使用本地关键词检索；接入 BGE/兼容服务时填写）
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=BAAI/bge-m3

# 轻量本地向量数据库，无需独立服务或 Token。
VECTOR_STORE=qdrant
QDRANT_PATH=storage/qdrant

# 安全限制
MAX_UPLOAD_MB=20
QUERY_ROW_LIMIT=500
SQL_TIMEOUT_SECONDS=15
